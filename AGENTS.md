# 项目协作说明

本项目用于准备 [Nordic AI Cup 2026](https://github.com/amboltio/Nordic-AI-Cup-2026)。目前先研究 `medical-appointment/`。开始工作前先读 [WORKLOG.md](WORKLOG.md)，确认已完成的事和下一步。

## 工作原则

- 动手前说清目标、必要假设和验收方式；影响方案的疑问及时提出。
- 只改本次任务相关的内容，优先用简单方案，不顺手重构别的代码。
- 修改后做与改动相称的验证；没有跑过的测试不要写成“已通过”。
- 每次有实质进展时，在 `WORKLOG.md` 追加一条简短记录：做了什么、如何验证、下一步或阻碍。不要记录每条命令或聊天过程。
- 不把 API key、虚拟环境、模型权重、音频数据或机器专属路径提交到 GitHub。路径写法要让队友能在自己的机器上配置。

## 比赛约束

- 医疗题的 `/predict` 要在本机或自管服务器完成推理，评测请求过程中不能调用云端 AI API。
- 每段录音对应 10 道题；答案和证据时间段必须按题目顺序返回。以赛题 README 和 `dtos.py` 为接口准则。
- 本机 `medical-appointment/data` 是指向 E 盘的目录链接；它不会随 Git 正常复制。队友需要在自己的机器上准备数据目录。

`.codex/hooks.json` 是第三层自动检查配置：Codex 结束任务时检查交接文件是否存在，以及 Git 是否收进本地数据。它只提醒，不自动修改文件，也不能代替上面的协作规则或操作记录。
